public class Driver {

	public static void main(String[] args) {
		/*
		Scanner in = new Scanner(System.in);

		int choice = 0;
		String gameName = "";
		String gameGenre = "";
		int gameRating = 0;
		String releaseDate = "";

		GameList list = new GameList();

		while (choice == 0) {

			System.out.println("Enter '0' to add a game, enter 1 to view games: ");
			choice = in.nextInt();

			in.nextLine(); // nextLine() is here because otherwise the next input does not work

			while (choice == 0) {
				Game game = new Game();

				System.out.println("Enter Name of the game or '1' to go back to the menu: ");
				gameName = in.nextLine();
				
				if (gameName.equals("1")) {
					choice = 1;
				}

				else {
					System.out.println("Enter the genre of the game: ");
					gameGenre = in.nextLine();

					System.out.println("Enter the release date: ");
					releaseDate = in.nextLine();

					System.out.println("Enter the rating of the game: ");
					gameRating = in.nextInt();

					game.setName(gameName);
					game.setGenre(gameGenre);
					game.setRating(gameRating);
					game.setReleaseDate(releaseDate);

					list.addLast(game);

					System.out.println(list.print());
				}

			}
			choice = 0;

		}
		*/
	}
}
